import 'package:flutter/material.dart';

class AppColors {
  // Фоны
  static const background   = Color(0xFF151515); // Основной фон
  static const panel        = Color(0xFF2A2A2A); // Bottom панели
  static const inputFilled  = Color(0xFF696969); // Заполненные поля/кнопки
  static const inputOutline = Color(0xFFE4E4E4); // Кнопки только обводка

  // Текст
  static const textPrimary  = Color(0xFFFFFFFF);
  static const textMuted    = Color(0xFFB4B4B4);
  static const textSubtitle = Color(0xFFD4D4D4);

  // Чат
  static const userBubble   = Color(0xFF1F1F1F);

  // Акценты
  static const quickSuggest = Color(0xFFD9D9D9);
  static const sendButton   = Color(0xFFAEAEAE);
}